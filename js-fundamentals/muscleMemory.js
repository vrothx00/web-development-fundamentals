function sayHello(name) {
	let msg = "Hello, " + name + ". How are you?";
	return msg;
  }
  
  function sayGoodbye(name) {
	  let msg = "Goodbye " + name + ".";
	  return msg;
  }
  
  console.log(sayHello("Kay"));
  console.log(sayGoodbye("Kay"));